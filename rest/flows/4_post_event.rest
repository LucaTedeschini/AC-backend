@baseUrl = http://localhost:5000
@contentType = application/json


### Lobby Details (check if lobby is not disabled and event is finished)

### If yes send the feedback
POST {{baseUrl}}/api/v0/collections/feedbacks/ HTTP/1.1
Content-Type: {{contentType}}

{
    "rating": 5,
    "lobbysId": 1,
    "memberIds": [
       2
    ],
    "memberId": 2
}

###TODO  When lobby is disabled, send the feedbacks to the ML instance???